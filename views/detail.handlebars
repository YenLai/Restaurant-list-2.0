{{!-- partial template --}}
<div class="container mt-3">
  <div class="row">
    <h3 class="col-12 col-md-10 mx-auto py-2" style="background-color: #F4CB4F;">
      {{restaurant.name}}
    </h3>
    <div class=" col-12 col-md-10 mx-auto" style="color: white;">

      <p class="mb-1">
        <span>
          <i class="fas fa-filter pr-1"></i>
          類別：
        </span>
        {{restaurant.category}}
      </p>

      <p class="mb-1">
        <span>
          <i class="fas fa-map-marker-alt pr-2"></i>
          地址：
        </span>
        {{restaurant.location}}
        <a href="{{restaurant.google_map}}" class="text-secondary" target="_blank">
          <i class="fas fa-location-arrow pr-2 fa-xs"></i>
        </a>
      </p>

      <p class="mb-1">
        <span>
          <i class="fas fa-mobile-alt pr-2"></i>
          電話：
        </span>
        {{restaurant.phone}}
      </p>

      <p class="mb-1">
        <span>
          <i class="fas fa-comments pr-0"></i>
          描述：
        </span>
      <div>
        {{restaurant.description}}
      </div>

      </p>
      {{#if del}}
      <h3 class="text-center mb-3" style="color: salmon;">
        你確定要刪除此筆餐廳資料，刪除後不可復原。
      </h3>
      {{/if}}

      <div class="mb-3 text-center">
        <a href="/">
          <button class=" btn btn-outline-light" style="border-color: #F4CB4F; ">返回</button>
        </a>
        {{#if detail}}
        <a href="/restaurant/{{restaurant._id}}/edit">
          <button class="mx-3 btn btn-outline-light" style="border-color: #F4CB4F; ">修改</button>
        </a>
        <a href="/restaurant/{{restaurant._id}}/delete" class="mx-auto btn btn-outline-light"
          style="border-color: #F4CB4F; ">
          刪除
        </a>
        {{/if}}
        {{#if del}}
        <form action="/restaurant/{{restaurant._id}}/delete" method="post" style="display: inline;">
          <button class="mx-auto btn btn-outline-light" style="border-color: #F4CB4F;" type="submit">刪除</button>
        </form>
        {{/if}}

      </div>



      <img class="rounded mx-auto d-block mb-4 w-100" src="{{restaurant.image}}" alt="{{restaurant.name}}"
        style="max-width: 600px;">
    </div>
  </div>
</div>